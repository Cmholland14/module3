function getGreeting(name) {
return 'Hello ' + name + '!'}

A - function expression syntax
function get greeting = function (name) {
  return 'Hello ' + name + '!';
  };

B Arrow Function Syntax

const getGreeting = (name) => 'Hello ' + name + '!'; 